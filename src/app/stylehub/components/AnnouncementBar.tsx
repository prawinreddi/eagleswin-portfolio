"use client";

export default function AnnouncementBar() {
  return (
    <div className="bg-black text-white text-xs font-medium uppercase tracking-[0.2em] py-2 text-center w-full relative z-[60]">
      Free Express Shipping on all orders over ₹4,999
    </div>
  );
}
